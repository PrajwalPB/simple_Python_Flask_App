pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello its working'
            }
        }
    }
    stages {
        stage('build docker image') {
            steps {
                sh 'docker build -t test-image .'
            }
        }
    }
}
